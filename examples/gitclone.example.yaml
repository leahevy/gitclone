dest: ./

autofetch:
  -
    github:
      user: GITHUB_USER
      method: ssh
      token: GITHUB_TOKEN
      private: true
      path: "github.com/{user}/{repo}"


repositories:
  - git://git.kernel.org/pub/scm/linux/kernel/git/torvalds/linux.git kernel/linux
